'use strict';

var should = require('chai').should();

const Heap = require('../scripts/heap.js');

describe('Set', function () {

  let heap;

  beforeEach(function () {
    heap = new Heap();
  });


});
